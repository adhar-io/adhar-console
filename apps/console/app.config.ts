/**
 * Re-exports vite config for environments expecting the legacy `app.config.ts`
 * location used by earlier TanStack Start versions. Safe to remove once the
 * installed TanStack Start version is confirmed to read `vite.config.ts` only.
 */
export { default } from './vite.config.ts'
